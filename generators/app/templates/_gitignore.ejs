# System Files
.DS_Store
Thumbs.db

<%_ if (projectType === 'bedrock') { -%>
# Application
web/app/plugins/*
!web/app/plugins/.gitkeep
web/app/mu-plugins/*/
web/app/upgrade
web/app/uploads/*
!web/app/uploads/.gitkeep

# WordPress
web/wp
web/.htaccess
<%_ } -%>

# Logs
*.log

# Dotenv
.env
.env.*
!.env.example

# Composer
vendor
!src/**/vendor

<%_ if (projectType === 'bedrock') { -%>
# WP-CLI
wp-cli.local.yml
<%_ } -%>

<%_ if (projectType === 'craft') { -%>
# Craft files
backups
composer-backups
config-backups
logs
runtime
web/cpresources
<%_ } -%>

# npm
node_modules

# temporary build files
.tmp
src/scss/.tmp

<%_ if (projectType === 'bedrock' || projectType === 'craft') { -%>
# Ignore frontend files that are part of `gulp build` process
faviconData.json
<%_ } -%>
<%_ if (projectType === 'bedrock') { -%>
web/app/theme/<%= wordpressTemplateName %>/static

# Make sure ACF pro plugin is tracked by git
!web/app/plugins/advanced-custom-fields-pro
<%_ } else if (projectType === 'craft') { -%>
web/assets
<%_ } -%>
